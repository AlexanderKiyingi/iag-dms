# iag-dms

Document management system (core) microservice for the IAG platform.

Reserved scaffold — implementation will follow platform patterns used by `iag-fleet` and `iag-procurement` (gateway auth, Kafka events, shared notifications).

Registry: [`subrepos.json`](../../../subrepos.json) · Planned dev port: **4009** (confirm before deploy — `iag-procurement` also uses 4009 today)
